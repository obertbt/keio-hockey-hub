import { redirect } from 'next/navigation';

/**
 * 0026 でスキルの申請と承認をやめた。
 *
 * 「大分類 → 中目標 → 小目標」の3段階と、
 * 小目標ごとの申請・審査・承認をたたんで、
 * 「大分類（チームで固定）+ 中目標（各自が書く）」の2段階にした。
 *
 * この道は残しておく。**古いリンクや、覚えている人のブックマークを
 * 行き止まりにしない。** 黙って 404 を返すより、行き先へ送る。
 */
export default function SkillsPage() {
  redirect('/goals');
}
